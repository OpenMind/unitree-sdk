version: '3.8'

services:
  zenoh_bridge:
    image: eclipse/zenoh-bridge-ros2dds:latest
    container_name: zenoh_bridge
    network_mode: host
    privileged: true
    environment:
      - ROS_DISTRO=humble
    volumes:
      - ./zenoh/zenoh_bridge_config.json5:/config/zenoh_bridge_config.json5:ro
    command: "-c /config/zenoh_bridge_config.json5"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped

  om1_sensor:
    build:
      context: .
    image: openmindagi/unitree_go2_sdk:latest
    container_name: om1_sensor
    network_mode: host
    devices:
      - /dev/ttyUSB0:/dev/ttyUSB0
      - /dev/bus/usb:/dev/bus/usb
    privileged: true
    environment:
      - DISPLAY=${DISPLAY:-}
      - QT_X11_NO_MITSHM=1
      - ROS_DOMAIN_ID=0
      - CYCLONEDDS_HOME=/app/cyclonedds/install
      - CYCLONEDDS_URI=file:///cyclonedds/cyclonedds.xml
      - CYCLONE_INTERFACE=${CYCLONE_INTERFACE:-eno1}
      - OM_API_KEY=${OM_API_KEY:-}
      - OM_API_KEY_ID=${OM_API_KEY_ID:-}
    command: "ros2 launch go2_sdk sensor_launch.py"
    volumes:
      - /dev:/dev
      - /run/udev:/run/udev:ro
      - /sys:/sys:ro
      - ./cyclonedds:/cyclonedds
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped

  watchdog:
    build:
      context: .
    image: openmindagi/unitree_go2_sdk:latest
    container_name: watchdog
    network_mode: host
    privileged: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - SENSOR_TIMEOUT_DURATION=30.0
    command: "python3 watchdog/watch_sensor.py"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped

  orchestrator:
    build:
      context: .
    image: openmindagi/unitree_go2_sdk:latest
    container_name: orchestrator
    network_mode: host
    privileged: true
    environment:
      - DISPLAY=${DISPLAY:-}
      - QT_X11_NO_MITSHM=1
      - ROS_DOMAIN_ID=0
      - CYCLONEDDS_HOME=/app/cyclonedds/install
      - CYCLONEDDS_URI=file:///cyclonedds/cyclonedds.xml
      - CYCLONE_INTERFACE=${CYCLONE_INTERFACE:-eno1}
      - OM_API_KEY=${OM_API_KEY:-}
    command: "ros2 launch orchestrator orchestrator_launch.py"
    volumes:
      - /dev:/dev
      - /run/udev:/run/udev:ro
      - /sys:/sys:ro
      - ./cyclonedds:/cyclonedds
      - ./maps:/app/unitree_go2_ros2_sdk/maps:rw
      - ${HOME}/shared_data/locations:/app/unitree_go2_ros2_sdk/locations:rw
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
